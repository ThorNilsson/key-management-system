import BoxPopupHeader from "../../components/BoxPopupHeader"

export default function EditBoxView({ close }) {
	return (
		<>
			<BoxPopupHeader title={`Update information about`} close={close} />
		</>
	)
}
